<?xml version="1.0" encoding="utf-8"?>
<!-- Home-screen 3x1 widget with timer controls.

     NESTING IS SUPPORTED — CONFIRMED (v3.28, user-verified on device).
     An earlier note in this file claimed "this host rejects nested layouts
     (they render blank/kill clicks)". That was a MISDIAGNOSIS, made in the same
     session the buttons were dead for an unrelated reason: the missing
     PendingIntent mutability flag (targetSdk 31+ throws IllegalArgumentException,
     swallowed by a try/catch). Once that was fixed, nesting worked first try.
     Do NOT reintroduce the flat-single-row workaround on account of nesting.

     Structure:
       Root (vertical)
         +- jobName   : full-width name line across the top
         +- row       : horizontal container
              +- hoursText : weight 1
              +- timerBtn  : weight 1
              +- stopBtn   : weight 1

     Text/colors set at runtime. Root background is the solid dark panel
     (@drawable/widget_bg_dark, #1B1F1D, 24dp corners) so the letters never
     float over the wallpaper. Keep a concrete background declared in XML: on
     Android 12+ the launcher inspects the layout to identify a background — if
     none is declared it paints its own opaque system panel instead. -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/widgetRoot"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="@drawable/widget_bg_dark"
    android:padding="8dp">

    <TextView android:id="@+id/jobName"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="start|center_vertical"
        android:singleLine="true"
        android:ellipsize="end"
        android:textSize="14sp"
        android:text=""/>

    <LinearLayout android:id="@+id/row"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:orientation="horizontal"
        android:gravity="center_vertical">

        <TextView android:id="@+id/hoursText"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:gravity="start|center_vertical"
            android:singleLine="true"
            android:textSize="18sp"
            android:textStyle="bold"
            android:text=""/>

        <Button android:id="@+id/timerBtn"
            android:layout_width="0dp"
            android:layout_height="match_parent"
            android:layout_weight="1"
            android:layout_marginStart="2dp"
            android:gravity="center"
            android:minWidth="0dp"
            android:minHeight="0dp"
            android:insetTop="0dp"
            android:insetBottom="0dp"
            android:textSize="20sp"
            android:text="▶"/>

        <Button android:id="@+id/stopBtn"
            android:layout_width="0dp"
            android:layout_height="match_parent"
            android:layout_weight="1"
            android:layout_marginStart="2dp"
            android:gravity="center"
            android:minWidth="0dp"
            android:minHeight="0dp"
            android:insetTop="0dp"
            android:insetBottom="0dp"
            android:textSize="20sp"
            android:text="■"/>

    </LinearLayout>

</LinearLayout>